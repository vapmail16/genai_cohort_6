# Local Postgres (default): port 5432, same user/db as compose `warehouse` service.
# Full override:
#   export DATABASE_URL=postgresql+psycopg2://user:pass@127.0.0.1:PORT/dbname
#
# Docker Compose publishes host 5433 → container 5432; point the client there:
#   export WAREHOUSE_HOST_PORT=5433
